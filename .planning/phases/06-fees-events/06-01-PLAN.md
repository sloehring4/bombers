---
phase: 06-fees-events
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/fees.ts
  - src/components/fees/FeeCard.tsx
  - src/components/fees/EventList.tsx
  - src/components/fees/RegistrationCTA.tsx
autonomous: true

must_haves:
  truths:
    - "Fee data exists for multiple age groups with totals and breakdowns"
    - "Key dates data covers tryouts, registration, season start, and tournaments"
    - "FAQ data covers common parent questions about fees, tryouts, and season"
    - "FeeCard component displays age group name, total cost prominently, and itemized breakdown"
    - "EventList component displays dates with calendar icons and left border accent"
    - "RegistrationCTA component embeds JerseyWatch iframe with sandbox security and link fallback"
  artifacts:
    - path: "src/lib/data/fees.ts"
      provides: "Type-safe fee, date, and FAQ data"
      exports: ["AgeFee", "KeyDate", "FAQItem", "ageFees", "keyDates", "faqItems"]
    - path: "src/components/fees/FeeCard.tsx"
      provides: "Pricing card for age group fees"
      contains: "FeeCard"
    - path: "src/components/fees/EventList.tsx"
      provides: "Key dates display list"
      contains: "EventList"
    - path: "src/components/fees/RegistrationCTA.tsx"
      provides: "Registration iframe embed with fallback"
      contains: "iframe"
  key_links:
    - from: "src/components/fees/FeeCard.tsx"
      to: "src/lib/data/fees.ts"
      via: "AgeFee type import"
      pattern: "import.*AgeFee.*from.*data/fees"
    - from: "src/components/fees/EventList.tsx"
      to: "src/lib/data/fees.ts"
      via: "KeyDate type import"
      pattern: "import.*KeyDate.*from.*data/fees"
    - from: "src/components/fees/RegistrationCTA.tsx"
      to: "jerseywatch.com"
      via: "iframe src attribute"
      pattern: "iframe.*src.*jerseywatch"
---

<objective>
Create the fees & events data layer and all section components for the Fees & Events page.

Purpose: Provide type-safe data (fees by age group, key dates, FAQ questions) and presentational components (FeeCard, EventList, RegistrationCTA) that Plan 02 will compose into the full page.
Output: One data file and three components ready for page assembly.
</objective>

<execution_context>
@C:/Users/sloeh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sloeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fees-events/06-RESEARCH.md

# Prior phase references
@src/lib/data/home.ts (date format pattern, keyDates example)
@src/lib/data/teams.ts (data file pattern with interfaces and readonly arrays)
@src/components/organization/Accordion.tsx (AccordionItem interface for FAQ data mapping)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fees data file with types and content</name>
  <files>src/lib/data/fees.ts</files>
  <action>
Create `src/lib/data/fees.ts` following the centralized data file pattern from prior phases (see teams.ts, home.ts).

**Interfaces to define:**

1. `AgeFee` — ageGroup (string), breakdown (readonly array of {label: string, amount: number}), total (number), description (optional string)
2. `KeyDate` — id (string), label (string), date (string, display format like "March 15, 2026"), category ('tryout' | 'registration' | 'season' | 'tournament'), description (optional string)
3. `FAQItem` — id (string), question (string), answer (string), category ('fees' | 'tryouts' | 'season' | 'general')

**Data to populate:**

`ageFees` (readonly array): Create entries for 7U, 8U, 9U, 10U, 11U, 12U, 13U, 14U, 15U age groups. Use realistic placeholder amounts:
- 7U-8U: ~$350 total (season fee $250, uniform deposit $50, tournament fee $50)
- 9U-10U: ~$450 total (season fee $300, uniform deposit $75, tournament fee $75)
- 11U-12U: ~$550 total (season fee $350, uniform deposit $75, tournament fee $125)
- 13U-15U: ~$650 total (season fee $400, uniform deposit $100, tournament fee $150)
- Add a brief description for each tier (e.g., "Includes 2 tournaments" vs "Includes 3 tournaments")

`keyDates` (readonly array): Include 6-8 dates in chronological order:
- Tryouts: "March 15, 2026"
- Registration Deadline: "March 30, 2026"
- Season Starts: "April 12, 2026"
- Opening Day Jamboree: "April 18, 2026"
- Memorial Day Classic Tournament: "May 23-25, 2026"
- Mid-Season Tournament: "June 13-14, 2026"
- All-Star Weekend: "July 11-12, 2026"
- Season Ends: "July 25, 2026"

`faqItems` (readonly array): Generate 10-12 FAQ items grouped by category. Cover:
- **fees** (3-4 questions): What's included in fees, payment plans available, financial aid/scholarships, refund policy
- **tryouts** (2-3 questions): When/where tryouts happen, what to bring, age cutoff dates, skill level expectations
- **season** (2-3 questions): Practice schedule, game schedule, tournament travel, required equipment
- **general** (2-3 questions): Volunteer requirements, communication channels, weather cancellation policy

Use `as const` on exported arrays for readonly typing. Follow the exact pattern from teams.ts.
  </action>
  <verify>TypeScript compilation passes: `npx tsc --noEmit`. File exports AgeFee, KeyDate, FAQItem interfaces and ageFees, keyDates, faqItems arrays.</verify>
  <done>fees.ts contains complete type-safe data for all age group fees, 6-8 key dates, and 10-12 FAQ items across 4 categories.</done>
</task>

<task type="auto">
  <name>Task 2: Build FeeCard, EventList, and RegistrationCTA components</name>
  <files>
    src/components/fees/FeeCard.tsx
    src/components/fees/EventList.tsx
    src/components/fees/RegistrationCTA.tsx
  </files>
  <action>
Create three components in `src/components/fees/`:

**FeeCard.tsx (server component, no 'use client'):**
- Props: Accept an `AgeFee` object from fees.ts
- Layout: Card with border-2 border-gray-200 rounded-lg, hover:border-bombers-yellow transition
- Content hierarchy:
  1. Age group name as h3 (text-xl font-bold text-bombers-navy)
  2. Total cost prominently displayed (text-3xl font-bold text-bombers-navy, formatted as "$XXX")
  3. Optional description (text-sm text-gray-600)
  4. Itemized breakdown as list (flex justify-between for label/amount pairs, text-sm text-gray-700)
- Use DollarSign icon from lucide-react in the card header area
- Add padding p-6, background bg-white

**EventList.tsx (server component, no 'use client'):**
- Props: Accept `events: KeyDate[]` from fees.ts
- Layout: Vertical list with space-y-4
- Each event: flex gap-4 items-start, left border accent (border-l-4 border-bombers-yellow pl-4 py-2)
- Calendar icon from lucide-react (w-5 h-5 text-bombers-navy, flex-shrink-0)
- Event label as font-semibold text-bombers-navy
- Event date as text-gray-600
- Optional description below date in text-sm text-gray-500
- Add category badge: small pill with category name (bg-bombers-navy/10 text-bombers-navy text-xs px-2 py-0.5 rounded-full)

**RegistrationCTA.tsx (server component, no 'use client'):**
- Layout: bg-gray-50 border-2 border-gray-200 rounded-lg p-8
- Header: "Register Your Player" h3, text-2xl font-bold text-bombers-navy
- Brief intro paragraph explaining online registration
- "What to bring" checklist: 4-5 items (player info, medical forms, payment method, emergency contacts) using CheckCircle icons from lucide-react
- Iframe embed:
  - src="https://www.jerseywatch.com/org/ofallon-bombers/register" (placeholder URL, will need real URL)
  - sandbox="allow-scripts allow-forms allow-same-origin allow-popups"
  - className="w-full h-[600px] border-2 border-gray-300 rounded-lg"
  - title="Player Registration Form"
  - loading="lazy"
- Fallback link below iframe: "If the form doesn't load, register directly on JerseyWatch" with arrow, linking to same URL, target="_blank" rel="noopener noreferrer"
- Style link as text-bombers-navy font-semibold hover:text-bombers-yellow transition-colors
  </action>
  <verify>TypeScript compilation passes: `npx tsc --noEmit`. All three component files exist in src/components/fees/ and import types from @/lib/data/fees.</verify>
  <done>FeeCard renders age group pricing cards, EventList renders chronological key dates with icons, RegistrationCTA embeds JerseyWatch iframe with sandbox security and visible link fallback.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `src/lib/data/fees.ts` exports 3 interfaces and 3 data arrays
3. `src/components/fees/FeeCard.tsx` exists and imports AgeFee type
4. `src/components/fees/EventList.tsx` exists and imports KeyDate type
5. `src/components/fees/RegistrationCTA.tsx` exists and contains iframe with sandbox attribute
6. No 'use client' directives on server components (FeeCard, EventList, RegistrationCTA)
</verification>

<success_criteria>
- Data layer provides complete, typed content for fees (9 age groups), dates (6-8 events), and FAQ (10-12 items)
- FeeCard shows total prominently with itemized breakdown
- EventList renders dates with calendar icons and category badges
- RegistrationCTA embeds iframe with sandbox security and has visible fallback link
- All components compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-fees-events/06-01-SUMMARY.md`
</output>
