---
phase: 02-core-layout-navigation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/layout/Footer.tsx
  - src/app/layout.tsx
autonomous: false

must_haves:
  truths:
    - "Header with logo and navigation appears on all pages"
    - "Footer with quick links and contact info appears on all pages"
    - "Footer sticks to bottom on short-content pages"
    - "Navigation includes Home, Teams, About, Fees & Events, Spirit Wear, Contact"
    - "Mobile hamburger menu works correctly on small screens"
    - "Navigation active state highlights current page"
  artifacts:
    - path: "src/components/layout/Footer.tsx"
      provides: "Site-wide footer with quick links, contact info, attribution"
    - path: "src/app/layout.tsx"
      provides: "Root layout integrating Header, Footer, and sticky footer pattern"
      contains: "Header"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import and render in body"
      pattern: "import Header"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Footer.tsx"
      via: "import and render in body"
      pattern: "import Footer"
    - from: "src/app/layout.tsx"
      to: "children prop"
      via: "main element with flex-1 between Header and Footer"
      pattern: "flex-1.*children"
---

<objective>
Create the site footer and integrate Header + Footer into the root layout, completing the site-wide shell that wraps all pages.

Purpose: Every page gets consistent header navigation and footer with contact/links, creating the unified site experience parents expect.
Output: Footer.tsx (Server Component), updated layout.tsx with Header/Footer/sticky-footer pattern
</objective>

<execution_context>
@C:/Users/sloeh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sloeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-layout-navigation/02-01-SUMMARY.md
@src/app/globals.css
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Footer and integrate layout shell</name>
  <files>
    src/components/layout/Footer.tsx
    src/app/layout.tsx
  </files>
  <action>
1. Create `src/components/layout/Footer.tsx` — Server Component (NO 'use client'):
   - Import `Link` from `next/link`
   - Dark navy background: `bg-navy-900 text-white`
   - Outer wrapper: `<footer>` with `border-t border-navy-700`
   - Inner container: `mx-auto max-w-7xl px-4 py-12`
   - 3-column grid layout using `grid gap-8 md:grid-cols-3` (stacks on mobile, 3 cols on desktop):

   **Column 1 — Quick Links:**
   - Heading: `<h3>` with `mb-4 text-sm font-semibold uppercase tracking-wide text-navy-300`
   - Links: Teams, About, Fees & Events, Spirit Wear, Contact — each as `<Link>` with `text-sm text-navy-200 hover:text-bombers-yellow transition-colors`
   - Render as `<nav>` with `flex flex-col gap-2`

   **Column 2 — Contact:**
   - Heading: "Contact" same style as Column 1
   - Email: `ofallonbombers@gmail.com` as plain text
   - Location: `O'Fallon, Illinois` as plain text
   - Use `text-sm text-navy-200` for content

   **Column 3 — Follow Us:**
   - Heading: "Follow Us" same style as Column 1
   - Facebook link: `<a href="https://facebook.com/ofallonbombers" target="_blank" rel="noopener noreferrer">`
   - Instagram link: `<a href="https://instagram.com/ofallonbombers" target="_blank" rel="noopener noreferrer">`
   - Style: `text-sm text-navy-200 hover:text-bombers-yellow transition-colors`

   **Bottom bar — Attribution:**
   - Separator: `mt-8 border-t border-navy-700 pt-8`
   - Centered text: `text-center text-sm text-navy-400`
   - Line 1: "Powered by JerseyWatch" — link JerseyWatch to `https://jerseywatch.com` with `hover:text-bombers-yellow` and `target="_blank" rel="noopener noreferrer"`
   - Line 2: copyright using `{new Date().getFullYear()}` — "O'Fallon Bombers. All rights reserved." (use `&apos;` for apostrophe in O'Fallon)

2. Update `src/app/layout.tsx` — keep as Server Component:
   - Add imports: `import Header from '@/components/layout/Header'` and `import Footer from '@/components/layout/Footer'`
   - Update `<body>` className to: `flex min-h-screen flex-col bg-white text-navy-900 antialiased` (add flex and flex-col for sticky footer)
   - Remove `${inter.variable} ${poppins.variable}` from `<html>` — WAIT, keep these! They're needed for fonts.
   - Inside `<body>`, render: `<Header />`, then `<main className="flex-1">{children}</main>`, then `<Footer />`
   - Keep existing `<html lang="en" className={...}>` with font variables intact
   - Keep existing metadata export intact

**Design decisions (Claude's discretion):**
- Footer uses dark navy-900 background (contrasts with white body, professional look)
- 3-column layout: Quick Links, Contact, Follow Us (covers essential info)
- No sponsor logos in footer for now (can add in Phase 5 when sponsor data is available)
- JerseyWatch attribution included as subtle credit
- External links use target="_blank" with noopener noreferrer for security
- Sticky footer via flex min-h-screen flex-col on body + flex-1 on main
  </action>
  <verify>Run `pnpm build` — must complete without errors. Verify layout.tsx imports Header and Footer. Verify body has flex min-h-screen flex-col classes. Verify Footer.tsx contains Quick Links, Contact, and Follow Us sections. Verify out/index.html contains header and footer markup.</verify>
  <done>Footer renders with 3 columns (Quick Links, Contact, Follow Us) and attribution bar. Root layout wraps all pages with Header + main + Footer. Sticky footer pattern keeps footer at bottom on short pages. Build succeeds with header and footer in output HTML.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify header, footer, and navigation visually</name>
  <files>src/app/layout.tsx</files>
  <action>
What was built: Complete site-wide layout shell with sticky header (logo + responsive navigation), footer (3-column with quick links, contact, social), and sticky footer pattern. Desktop shows horizontal nav links; mobile (below 768px) shows hamburger menu.

How to verify:
1. Start dev server: `pnpm dev`
2. Open http://localhost:3000 in browser
3. **Desktop (>768px):**
   - Header: Logo on left, 6 navigation links on right (Home, Teams, About, Fees & Events, Spirit Wear, Contact)
   - "Home" link should be highlighted (active state) with yellow color and bottom border
   - No hamburger icon visible
   - Footer at bottom with 3 columns and attribution
4. **Mobile (resize to <768px or use DevTools responsive mode):**
   - Header: Logo on left, hamburger icon on right
   - No horizontal nav links visible
   - Tap hamburger: menu drops down with all 6 links
   - "Home" link highlighted in mobile menu
   - Tap a link: menu closes
   - Tap hamburger again to reopen, tap X to close
5. **Sticky footer:** Footer stays at bottom even though page content is short
6. **Scroll test:** Header stays sticky at top when scrolling (if content is long enough)

Resume signal: Type "approved" or describe any issues to fix
  </action>
  <verify>Visual inspection confirms header, footer, navigation, mobile menu, active state, and sticky footer all function correctly.</verify>
  <done>User has approved the layout shell visually — header, footer, navigation, and responsive behavior all meet expectations.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. Header appears with logo, desktop nav links, and mobile hamburger
3. Footer appears with Quick Links, Contact, Follow Us columns and attribution
4. Layout uses flex min-h-screen flex-col sticky footer pattern
5. Mobile hamburger toggles at 768px breakpoint
6. Active page is highlighted in navigation
7. All 6 navigation pages listed: Home, Teams, About, Fees & Events, Spirit Wear, Contact
</verification>

<success_criteria>
- Header with logo and navigation menu appears on every page via root layout
- Footer with essential links and contact info appears on every page via root layout
- Navigation includes 6 core pages
- Mobile hamburger menu works correctly below 768px
- Active navigation state highlights current page
- Footer stays at bottom on short-content pages
- Static build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-layout-navigation/02-02-SUMMARY.md`
</output>
