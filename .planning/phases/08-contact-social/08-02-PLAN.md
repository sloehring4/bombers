---
phase: 08-contact-social
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/contact/SocialFeed.tsx
  - src/app/contact/page.tsx
  - src/components/layout/Footer.tsx
autonomous: true

must_haves:
  truths:
    - "Contact page displays form, email, social links, donation link, and social feed"
    - "Social feed section shows embedded social media content or a clear placeholder with follow link"
    - "Donation link navigates to external donation page in new tab"
    - "Footer social links display with lucide-react icons"
    - "Contact page has SEO metadata (title and description)"
  artifacts:
    - path: "src/app/contact/page.tsx"
      provides: "Contact page composing all contact components"
      contains: "ContactForm"
    - path: "src/components/contact/SocialFeed.tsx"
      provides: "Social media feed embed or placeholder component"
      contains: "SocialFeed"
    - path: "src/components/layout/Footer.tsx"
      provides: "Updated footer with social media icons"
      contains: "lucide-react"
  key_links:
    - from: "src/app/contact/page.tsx"
      to: "src/components/contact/ContactForm.tsx"
      via: "component import"
      pattern: "import ContactForm"
    - from: "src/app/contact/page.tsx"
      to: "src/components/contact/SocialLinks.tsx"
      via: "component import"
      pattern: "import SocialLinks"
    - from: "src/app/contact/page.tsx"
      to: "src/lib/data/contact.ts"
      via: "data import for email and donation URL"
      pattern: "import.*CONTACT_EMAIL.*DONATION_URL"
---

<objective>
Create SocialFeed embed component, compose the /contact page with all sections, and enhance the footer with social media icons.

Purpose: Delivers the complete contact page where families can reach the organization, connect on social media, and make donations. Also upgrades footer with icon links.
Output: Working /contact page with form, social links, donation CTA, social feed, and enhanced footer.
</objective>

<execution_context>
@C:/Users/sloeh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sloeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-contact-social/08-RESEARCH.md
@.planning/phases/08-contact-social/08-01-SUMMARY.md
@src/components/layout/Footer.tsx (to enhance with icons)
@src/app/fees/page.tsx (reference for page structure with metadata export)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SocialFeed component and compose /contact page</name>
  <files>
    src/components/contact/SocialFeed.tsx
    src/app/contact/page.tsx
  </files>
  <action>
    1. Create `src/components/contact/SocialFeed.tsx` as a server component:
       - Since third-party embed services (EmbedSocial, SociableKIT) require account signup and configuration, implement a placeholder-ready component
       - Display a "Follow Our Journey" section with yellow bottom border heading accent (consistent with other pages)
       - Include a visually appealing placeholder state:
         - Card-style container with bg-gray-50 rounded-lg border-2 border-gray-200 p-8
         - Instagram and Facebook icons (from lucide-react) at large size (w-12 h-12) in a flex row
         - Text: "Follow us on social media for the latest updates, game highlights, and team news!"
         - Two CTA buttons linking to Facebook and Instagram pages (import socialLinks from contact data)
         - Buttons styled: one bg-bombers-navy text-white, one bg-bombers-yellow text-bombers-navy
         - Both buttons: px-6 py-3 rounded-lg font-semibold hover transitions, target="_blank"
       - Include an HTML comment in the component marking where embed widget code should be inserted when the organization sets up EmbedSocial/SociableKIT
       - The placeholder is production-ready (not ugly or broken-looking) — it serves as the social CTA section

    2. Create `src/app/contact/page.tsx` as a server component:
       - Export metadata: title "Contact Us | O'Fallon Bombers", description about contacting the organization
       - Import ContactForm, SocialLinks, SocialFeed from @/components/contact/
       - Import CONTACT_EMAIL, DONATION_URL from @/lib/data/contact
       - Import Mail, Heart (or Gift) from lucide-react for section icons
       - Page structure (within main tag, container mx-auto px-4 max-w-6xl py-8 md:py-12):

         a. Page header: h1 "Contact Us" (text-3xl md:text-4xl font-bold text-bombers-navy), subtitle paragraph

         b. Two-column grid (grid-cols-1 lg:grid-cols-2 gap-8 mb-12):
            - Left column: "Send Us a Message" h2 with yellow border accent, then <ContactForm />
            - Right column (aside, space-y-8):
              - "Email Us Directly" section: Mail icon + mailto link to CONTACT_EMAIL, small helper text
              - "Connect With Us" section: <SocialLinks />
              - "Support Our Team" section: Heart icon + description text about supporting youth baseball, then prominent CTA button linking to DONATION_URL (bg-bombers-yellow, target="_blank", rel="noopener noreferrer"), with ExternalLink icon

         c. Full-width social feed section below the grid: <SocialFeed />

       - All section headings use consistent pattern: text-2xl font-semibold text-bombers-navy mb-4 border-b-4 border-bombers-yellow pb-2
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds
    - Build output shows /contact route generated
  </verify>
  <done>
    - /contact page renders with contact form, email link, social links, donation CTA, and social feed
    - Page has proper SEO metadata exported
    - Social feed section is production-ready (attractive placeholder with direct social links)
    - Two-column responsive layout collapses to single column on mobile
    - All section headings use yellow border accent pattern
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance footer with social media icons</name>
  <files>
    src/components/layout/Footer.tsx
  </files>
  <action>
    1. Update `src/components/layout/Footer.tsx`:
       - Import Facebook and Instagram from lucide-react
       - In the "Follow Us" column (column 3), add icons next to the social link text:
         - Each link becomes: flex items-center gap-2
         - Add <Facebook className="w-4 h-4" aria-hidden="true" /> before "Facebook" text
         - Add <Instagram className="w-4 h-4" aria-hidden="true" /> before "Instagram" text
       - Add aria-label attributes to social links: `aria-label="Visit our Facebook page"` etc.
       - Keep all existing footer structure, links, and styling intact
       - Note: Footer is a server component — lucide-react icons work fine in server components (they are SVG components, no client JS needed)
  </action>
  <verify>
    - `npm run build` succeeds without errors
    - Footer still renders correctly with all existing content plus icons
  </verify>
  <done>
    - Footer social links display with Facebook and Instagram icons
    - Icons have aria-hidden="true" for accessibility
    - Links have aria-label for screen readers
    - No changes to existing footer structure or other links
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- /contact route appears in build output
- Contact page imports and renders ContactForm, SocialLinks, SocialFeed
- Contact page exports metadata with title and description
- Donation link points to DONATION_URL with target="_blank"
- Footer social links have lucide-react icons
- All section headings use consistent yellow border accent
</verification>

<success_criteria>
- Contact page at /contact with all four requirement areas: form (CONT-01), social links (CONT-02), donation (CONT-03), social feed (CONT-04)
- SEO metadata on contact page
- Footer enhanced with social media icons
- Production-ready social feed section (works without third-party account)
- Build passes cleanly with /contact route generated
</success_criteria>

<output>
After completion, create `.planning/phases/08-contact-social/08-02-SUMMARY.md`
</output>
