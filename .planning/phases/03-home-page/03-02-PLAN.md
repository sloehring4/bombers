---
phase: 03-home-page
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/home/QuickLinkCard.tsx
  - src/components/home/QuickLinks.tsx
  - src/components/home/AnnouncementBanner.tsx
  - src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Quick link icon cards for Teams, Fees, Spirit Wear, and Contact are displayed in a responsive grid"
    - "Each quick link card has an icon, title, and description and navigates to the correct page"
    - "Announcement banner displays key dates (tryouts, registration deadline, season start)"
    - "Banner is dismissible and remembers dismissal via localStorage"
    - "Home page composes Hero, AnnouncementBanner, and QuickLinks in correct order"
    - "Page communicates athletic energy in hero and clean structure in content sections"
  artifacts:
    - path: "src/components/home/QuickLinkCard.tsx"
      provides: "Individual icon card with hover effects and link navigation"
      min_lines: 15
    - path: "src/components/home/QuickLinks.tsx"
      provides: "Responsive grid section composing QuickLinkCard items"
      min_lines: 15
    - path: "src/components/home/AnnouncementBanner.tsx"
      provides: "Dismissible key dates banner with localStorage persistence"
      min_lines: 25
    - path: "src/app/page.tsx"
      provides: "Composed home page with Hero + Banner + QuickLinks"
      min_lines: 10
  key_links:
    - from: "src/components/home/QuickLinks.tsx"
      to: "src/lib/data/home.ts"
      via: "import quickLinks"
      pattern: "import.*quickLinks.*from.*@/lib/data/home"
    - from: "src/components/home/QuickLinks.tsx"
      to: "src/components/home/QuickLinkCard.tsx"
      via: "import QuickLinkCard"
      pattern: "import.*QuickLinkCard"
    - from: "src/components/home/AnnouncementBanner.tsx"
      to: "src/lib/data/home.ts"
      via: "import KeyDate type"
      pattern: "import.*KeyDate.*from.*@/lib/data/home"
    - from: "src/app/page.tsx"
      to: "src/components/home/Hero.tsx"
      via: "import Hero"
      pattern: "import.*Hero.*from.*@/components/home/Hero"
    - from: "src/app/page.tsx"
      to: "src/components/home/QuickLinks.tsx"
      via: "import QuickLinks"
      pattern: "import.*QuickLinks.*from.*@/components/home/QuickLinks"
    - from: "src/app/page.tsx"
      to: "src/components/home/AnnouncementBanner.tsx"
      via: "import AnnouncementBanner"
      pattern: "import.*AnnouncementBanner.*from.*@/components/home/AnnouncementBanner"
---

<objective>
Build the quick link cards, announcement banner, and compose the complete home page. QuickLinkCard provides icon cards with hover effects. QuickLinks arranges them in a responsive grid. AnnouncementBanner shows key dates with dismiss capability. page.tsx assembles everything.

Purpose: Completes the home page so parents can quickly navigate to Teams, Fees, Spirit Wear, and Contact, and see upcoming key dates at a glance.
Output: QuickLinkCard.tsx, QuickLinks.tsx, AnnouncementBanner.tsx, updated page.tsx
</objective>

<execution_context>
@C:/Users/sloeh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sloeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-home-page/03-CONTEXT.md
@.planning/phases/03-home-page/03-RESEARCH.md
@.planning/phases/03-home-page/03-01-SUMMARY.md
@src/app/globals.css
@src/lib/data/home.ts
@src/components/home/Hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QuickLinkCard, QuickLinks, and AnnouncementBanner components</name>
  <files>src/components/home/QuickLinkCard.tsx, src/components/home/QuickLinks.tsx, src/components/home/AnnouncementBanner.tsx</files>
  <action>
    1. Create `src/components/home/QuickLinkCard.tsx` (Server Component):
       - Props interface: `{ icon: LucideIcon; title: string; description: string; href: string }`
       - Render as `<Link href={href}>` wrapping a card div
       - Card styling: `p-8 bg-white rounded-xl border-2 border-gray-200` with group hover
       - Hover effects (per user decision -- icon cards with hover):
         - `hover:border-bombers-yellow hover:shadow-xl hover:-translate-y-1` on card
         - Icon container: circular background `bg-bombers-yellow/10 rounded-full` that transitions to `bg-bombers-yellow` on group-hover
         - Icon color: `text-bombers-navy` transitioning to `text-white` on group-hover
       - Add `active:scale-95` for mobile touch feedback
       - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-bombers-yellow` for keyboard accessibility
       - Icon size: 40, strokeWidth: 2
       - Title: `text-xl font-semibold text-bombers-navy`
       - Description: `text-gray-600 text-sm leading-relaxed`
       - All transitions: `transition-all duration-300`
       - Use `import type { LucideIcon } from 'lucide-react'` for type-only import

    2. Create `src/components/home/QuickLinks.tsx` (Server Component):
       - Import `quickLinks` from `@/lib/data/home`
       - Import `QuickLinkCard` from `./QuickLinkCard`
       - Render a section with light gray background: `py-16 bg-gray-50`
       - Container: `container mx-auto px-4`
       - Responsive grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`
       - Map over quickLinks to render QuickLinkCard for each
       - Use `link.title` as key

    3. Create `src/components/home/AnnouncementBanner.tsx` ('use client' component):
       - Must have `'use client'` directive (uses useState, useEffect, localStorage)
       - Props: `{ dates: { label: string; date: string }[]; bannerId: string }`
       - Import `{ useState, useEffect }` from 'react'
       - Import `{ X, Calendar }` from 'lucide-react'
       - Hydration-safe pattern:
         - Initialize `isVisible` to `false`
         - In useEffect, check `localStorage.getItem('banner-dismissed-' + bannerId)` -- if NOT dismissed, set visible to true
         - handleDismiss: set localStorage item and set visible to false
         - If !isVisible, return null
       - Banner styling per user decision (eye-catching strip):
         - `bg-bombers-yellow border-y-4 border-bombers-navy py-4 px-4`
         - Container inside: `container mx-auto flex items-center justify-between gap-4`
         - Dates displayed inline with flex-wrap: each date shows Calendar icon (size 18), bold label, and date string
         - Dismiss X button: `text-bombers-navy hover:bg-bombers-navy/10 p-2 rounded-full` with aria-label="Dismiss announcement"
         - Add focus-visible ring on dismiss button for accessibility
       - Note: Dates prop uses simple `{ label: string; date: string }` -- do NOT pass LucideIcon in props (would break serialization). Use Calendar icon directly in the component for all dates.
  </action>
  <verify>`pnpm build` succeeds. All three component files exist with correct imports and no TypeScript errors.</verify>
  <done>QuickLinkCard renders icon cards with hover effects. QuickLinks renders responsive 4-column grid. AnnouncementBanner shows key dates in yellow strip with dismiss button and localStorage persistence. No hydration mismatches.</done>
</task>

<task type="auto">
  <name>Task 2: Compose home page with all sections</name>
  <files>src/app/page.tsx</files>
  <action>
    Replace the existing placeholder page.tsx with the composed home page:
    - Import Hero from '@/components/home/Hero'
    - Import AnnouncementBanner from '@/components/home/AnnouncementBanner'
    - Import QuickLinks from '@/components/home/QuickLinks'
    - Import { keyDates } from '@/lib/data/home'
    - Page structure (per user decision -- hero + quick links + key dates banner, minimal):
      ```
      <main>
        <Hero />
        <AnnouncementBanner dates={keyDates.map(d => ({ label: d.label, date: d.date }))} bannerId="spring-2026" />
        <QuickLinks />
      </main>
      ```
    - Banner placed between hero and quick links for maximum visibility without blocking hero impact (per research recommendation)
    - The keyDates from home.ts may include LucideIcon references -- strip those before passing to client component. Map to plain `{ label, date }` objects.
    - Do NOT wrap in Container -- hero is full-width, each section manages its own container
    - Remove all old placeholder content (logo image, "Foundation Complete" card, etc.)
  </action>
  <verify>`pnpm build` succeeds. page.tsx imports and renders Hero, AnnouncementBanner, and QuickLinks.</verify>
  <done>Home page renders hero section, announcement banner with key dates, and quick link cards in correct order. Old placeholder content is removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of home page</name>
  <files>src/app/page.tsx</files>
  <action>
    Start dev server with `pnpm dev` and present the home page for user verification. User will check hero section, announcement banner, quick link cards, responsive behavior, and overall page composition.
  </action>
  <verify>User approves visual appearance and functionality of home page.</verify>
  <done>User confirms home page looks correct with hero, banner, and quick links all rendering properly at all breakpoints.</done>
  <what-built>Complete home page with hero section (full-width background, gradient overlay, headline, tagline, CTAs), announcement banner (yellow strip with key dates and dismiss button), and quick link cards (4 icon cards in responsive grid with hover effects).</what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev`
    2. Visit http://localhost:3000
    3. Verify hero section:
       - Full-width background image with dark gradient overlay
       - "O'Fallon Bombers" headline is large, bold, readable
       - Tagline text below headline
       - Two CTA buttons visible (Register Your Player, View Our Teams)
    4. Verify announcement banner:
       - Yellow strip between hero and quick links
       - Shows 3 key dates (Tryouts, Registration Deadline, Season Start)
       - X button dismisses the banner
       - Refresh page -- banner stays dismissed
       - Clear localStorage and refresh -- banner returns
    5. Verify quick links:
       - 4 icon cards in a grid (1 col mobile, 2 cols tablet, 4 cols desktop)
       - Each card has icon, title, description
       - Hover: border turns yellow, card lifts, icon background fills yellow
       - Click navigates to correct route
    6. Resize browser to test responsive behavior at mobile/tablet/desktop widths
    7. Verify header and footer still appear correctly from Phase 02
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. Home page at / renders Hero, AnnouncementBanner, and QuickLinks
3. Hero shows full-width image with overlay, headline, tagline, 2 CTAs
4. Banner shows 3 key dates and is dismissible with localStorage persistence
5. Quick links show 4 icon cards in responsive grid with hover effects
6. No hydration mismatch warnings in console
7. Header and footer from Phase 02 still render correctly
8. Page is mobile-responsive
</verification>

<success_criteria>
- Home page displays hero section with Bombers branding, tagline, and CTAs
- Quick link cards provide scannable navigation to Teams, Fees, Spirit Wear, Contact
- Key dates are visible in announcement banner without scrolling past hero
- Banner dismissal persists across page refreshes
- Page responsive at mobile, tablet, and desktop breakpoints
- Static build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-page/03-02-SUMMARY.md`
</output>
