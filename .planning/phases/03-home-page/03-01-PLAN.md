---
phase: 03-home-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - public/images/hero-placeholder.jpg
  - src/lib/data/home.ts
  - src/components/home/Hero.tsx
autonomous: true

must_haves:
  truths:
    - "Hero section displays full-width background image with gradient overlay"
    - "O'Fallon Bombers headline is large, bold, and readable over image"
    - "Tagline/motto appears below the headline"
    - "CTA buttons for registration and teams are visible in hero"
    - "Home page data (quick links, key dates, hero content) is type-safe and centralized"
  artifacts:
    - path: "src/components/home/Hero.tsx"
      provides: "Full-width hero section with image, overlay, headline, tagline, CTAs"
      min_lines: 25
    - path: "src/lib/data/home.ts"
      provides: "Type-safe data for hero content, quick links, and key dates"
      exports: ["quickLinks", "keyDates", "heroContent"]
    - path: "public/images/hero-placeholder.jpg"
      provides: "Placeholder hero background image"
  key_links:
    - from: "src/components/home/Hero.tsx"
      to: "src/lib/data/home.ts"
      via: "import heroContent"
      pattern: "import.*heroContent.*from.*@/lib/data/home"
    - from: "src/components/home/Hero.tsx"
      to: "src/components/ui/Button.tsx"
      via: "import Button"
      pattern: "import.*Button.*from.*@/components/ui/Button"
---

<objective>
Create the hero section foundation for the home page: install lucide-react icon library, set up type-safe home page data, generate a placeholder hero image, and build the full-width Hero component with gradient overlay and CTAs.

Purpose: Establishes the visual anchor of the site -- the hero is the first thing visitors see and must communicate Bombers identity with bold, sporty energy.
Output: Hero.tsx component, home.ts data file, placeholder hero image, lucide-react installed.
</objective>

<execution_context>
@C:/Users/sloeh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sloeh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-home-page/03-CONTEXT.md
@.planning/phases/03-home-page/03-RESEARCH.md
@src/app/globals.css
@src/components/ui/Button.tsx
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react and create home page data file</name>
  <files>package.json, src/lib/data/home.ts</files>
  <action>
    1. Install lucide-react:
       ```
       pnpm add lucide-react
       ```

    2. Create `src/lib/data/home.ts` with type-safe exports:
       - Define `QuickLink` interface: `{ icon: LucideIcon; title: string; description: string; href: string }`
       - Define `KeyDate` interface: `{ label: string; date: string }`
       - Export `quickLinks` array with 4 items:
         - Teams (Users icon, href: /teams, description about age divisions and rosters)
         - Fees & Registration (DollarSign icon, href: /fees, description about costs)
         - Spirit Wear (Shirt icon, href: /spirit-wear, description about official gear)
         - Contact Us (Mail icon, href: /contact, description about getting in touch)
       - Export `keyDates` array with 3 items:
         - Next Tryouts: March 15, 2026
         - Registration Deadline: March 30, 2026
         - Season Starts: April 12, 2026
       - Export `heroContent` as const object:
         - headline: "O'Fallon Bombers"
         - tagline: something fitting that captures team spirit and community (Claude writes this)
         - primaryCta: { text: "Register Your Player", href: "/fees" }
         - secondaryCta: { text: "View Our Teams", href: "/teams" }
       - Use named imports for Lucide icons: `import { Users, DollarSign, Shirt, Mail } from 'lucide-react'`
       - Import `type { LucideIcon }` separately for the interface
  </action>
  <verify>TypeScript compiles without errors: `pnpm exec tsc --noEmit`</verify>
  <done>lucide-react is in package.json dependencies. home.ts exports quickLinks (4 items), keyDates (3 items), and heroContent with proper TypeScript types.</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder hero image and Hero component</name>
  <files>public/images/hero-placeholder.jpg, src/components/home/Hero.tsx</files>
  <action>
    1. Create placeholder hero image at `public/images/hero-placeholder.jpg`:
       - Generate a simple gradient image using a canvas approach, OR use a solid dark-toned placeholder (navy/dark gradient) that works well under the overlay
       - The simplest approach: create a small solid-color JPEG (e.g., dark navy blue) as placeholder. It will be replaced with a real team photo later.
       - Can use Node.js script or download a free stock sports/baseball image. A simple dark placeholder is fine since the overlay will cover it.

    2. Create `src/components/home/Hero.tsx` as a Server Component (no 'use client'):
       - Import `Image` from 'next/image'
       - Import `heroContent` from '@/lib/data/home'
       - Import `Button` from '@/components/ui/Button'
       - Import `Link` from 'next/link'
       - Structure:
         ```
         <section className="relative h-[70vh] min-h-[500px] w-full flex items-center justify-center overflow-hidden">
           <Image src="/images/hero-placeholder.jpg" alt="O'Fallon Bombers baseball action" fill preload={true} sizes="100vw" quality={90} className="object-cover" />
           <div className="absolute inset-0 bg-gradient-to-b from-bombers-navy/70 via-bombers-navy/50 to-bombers-navy/70" />
           <div className="relative z-10 text-center px-4 max-w-4xl space-y-6">
             <h1> (text-5xl md:text-6xl lg:text-7xl font-bold text-white font-heading)
             <p> tagline (text-xl md:text-2xl text-white/90 font-medium)
             <div> flex gap for CTA buttons
           </div>
         </section>
         ```
       - CTA buttons: Wrap Button components in Link components for navigation. Primary CTA "Register Your Player" links to /fees. Secondary CTA "View Our Teams" links to /teams.
       - Use `font-heading` class on h1 to ensure Poppins font per design system
       - Use `O&apos;Fallon` for the apostrophe (JSX escaping)
       - Do NOT use static import for the hero image (use string path) since placeholder will be swapped later. This means `placeholder="blur"` cannot be used with string src -- omit it.
       - Note: Button component currently renders `<button>` elements. Wrap each in a `<Link>` tag, or use an anchor-styled approach. Simplest: use `<Link>` with Button-like styling classes directly, OR wrap `<Button>` in `<Link>`. The cleanest approach is wrapping Link around Button with an `asChild`-like pattern -- but since Button is simple, just wrap: `<Link href={heroContent.primaryCta.href}><Button variant="primary">{heroContent.primaryCta.text}</Button></Link>`
  </action>
  <verify>`pnpm build` succeeds without errors. Hero.tsx imports heroContent from data file and renders headline, tagline, and CTA buttons.</verify>
  <done>Hero component renders full-width background image section with gradient overlay, "O'Fallon Bombers" headline, tagline, and two CTA buttons. Placeholder image exists at public/images/hero-placeholder.jpg.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. `lucide-react` appears in package.json dependencies
3. `src/lib/data/home.ts` exports quickLinks (4 items), keyDates (3 items), heroContent
4. `src/components/home/Hero.tsx` exists with Image, gradient overlay, h1, tagline, CTAs
5. `public/images/hero-placeholder.jpg` exists
</verification>

<success_criteria>
- lucide-react installed and importable
- Home page data file provides type-safe content for all home page sections
- Hero component renders full-width with background image, navy gradient overlay, bold headline, tagline, and two CTA buttons
- Build succeeds with no TypeScript or Next.js errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-page/03-01-SUMMARY.md`
</output>
